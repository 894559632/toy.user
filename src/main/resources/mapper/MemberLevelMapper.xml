<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ynyes.cqhome.toy.dao.MemberLevelDao">
	<select id="findOne" parameterType="Long" resultType="MemberLevel">
		SELECT *
		FROM member_level WHERE id = #{id}
	</select>
</mapper>
